# 系统设计

---

# 第一部分 总

## 1.1 一句话

老银行系统无 API,只能驱动 UI。让 LLM 跑一次得到一份 artifact,生产期照 artifact 确定性执行,不调模型。

## 1.2 核心矛盾

LLM 每次实时决策,四个问题:

| 问题 | 后果 |
|---|---|
| 贵 | 每次调用重复付截图和推理成本 |
| 慢 | 每步 2 到 5 秒,一个流程几十秒 |
| 不可复现 | 今天点对明天可能点错 |
| 不可审计 | 无法向合规解释一笔操作为什么这样做 |

但这些系统的界面几年不变。既然不变,重复判断就是纯浪费。

**解法**:把"判断"和"执行"在时间上分开。判断一次,执行无数次。

## 1.3 七条设计原则

后面所有决策都从这七条推出。面试被追问任何一处,都可以回到这里。

| # | 原则 | 由它推出的决策 |
|---|---|---|
| P1 | **可移植优先于精确** | a11y 优先于 DOM;seam 之上不出现浏览器概念 |
| P2 | **确定性不可让渡** | 定位有序不打分;无裸 sleep;不运行时自愈;回放不调模型 |
| P3 | **观察优于断言** | 无静态 confidence;可靠性靠统计;异常靠跑出来而非问模型 |
| P4 | **业务结果不是故障** | 三分结果契约;terminal 先匹配 |
| P5 | **权限不由数据自报** | 护栏全在执行器;risk_level 执行器覆写;上界不由 artifact 定 |
| P6 | **做不下去就停下报告,不猜** | 升级而非自愈;resume 只查两个候选;拒绝自动重登 |
| P7 | **简单优先,但缝要留好** | 单进程、文件存储、CLI;同时四个 seam 全部就位 |

P2 和 P3 是一对:确定性要求执行路径固定,所以任何"当场评估选最优"的机制都不能进主路径;可靠性信息改为事后统计,不进 artifact。

P5 和 P6 是一对:前者防越权,后者防猜错。两者共同的立场是**系统宁可少做,不要做错**。

## 1.4 全局架构

**A. Discovery 产出 artifact**

```
 goal + target
      │
      ▼
 ┌────────────┐   observe   ┌───────┐
 │ Agent Loop │ ──────────► │  LLM  │
 │            │ ◄────────── │       │
 └─────┬──────┘   decide    └───────┘
       │ act
       ▼
 SurfaceAdapter ──► 浏览器
       │
       │ 目标达成
       ▼
 ┌────────────┐
 │ Hardening  │  坏参数再跑一次,补出 runtime_matches
 └─────┬──────┘
       │ 写
       ▼
 ╔════════════╗
 ║  ARTIFACT  ║
 ╚════════════╝
```

**B. Replay 消费 artifact,无模型**

```
 capability_id + params
       │
       ▼
 ╔════════════╗   读   ┌────────────────┐
 ║  ARTIFACT  ║ ─────► │ Replay Engine  │  逐步执行 + 断言 checkpoint
 ╚════════════╝        └───────┬────────┘
                               │ act
                               ▼
                        SurfaceAdapter ──► 浏览器
                               │
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
       SUCCESS          BUSINESS_OUTCOME         FAILURE
      + outputs          (查无此人)          + step/expected/observed
                                                   │
                                                   ▼
                                              Escalation
```

**C. 共用层**

```
 Agent Loop / Replay Engine
       │  只说 role / accessible name / label / text
       ▼
 ┌──────────────────────────────────────────┐
 │ SurfaceAdapter (Protocol)                │  ← seam
 ├────────────┬───────────────┬─────────────┤
 │ WebAdapter │ DesktopAdapter│VisionAdapter│
 │ Playwright │  只留接口      │  只留接口    │
 └─────┬──────┴───────────────┴─────────────┘
       │
       ▼
 Live Session(一个 headed 浏览器)
       ▲
       │ 人直接点这个已经开着的窗口
 ┌───────────┐
 │ Operator  │
 └───────────┘

 横切三处,位置各不同:
   PolicyGate     动作执行之前
   Redaction      写盘之前
   ControlBroker  SQLite,记录谁在控制
```

两条路径只通过 artifact 耦合:A 写,B 读,互不调用。删掉 `src/discovery/` 回放不受影响。这是 P7 里"缝要留好"的验收标准。

## 1.5 技术栈

| 层 | 选型 | 由哪条原则决定 |
|---|---|---|
| 语言 | Python 3.12 | 生态 |
| 浏览器 | Playwright sync,headed | headed 是为接管,见 5.3 |
| 感知 | a11y snapshot 优先,DOM 富化,截图存证 | P1 |
| Schema | Pydantic v2 | 契约即校验器 |
| 假银行 | FastAPI + Jinja2,服务端渲染 | 可控地制造异常,见 4.4 |
| 模型 | Groq 上的开源权重模型,走 OpenAI 兼容接口;藏在 `LLMProvider` 接口后 | P7,见 1.8 |
| CLI | Typer | P7 |
| 日志 | structlog → JSONL | 可 grep,可分析 |
| artifact / evidence | 磁盘文件 | git 即版本控制 + 可审阅 |
| 控制权状态 | SQLite | 需要跨进程条件原子更新,见 5.4 |
| 测试 | pytest | 只测四样,见 1.7 |

**四个 seam**:`SurfaceAdapter` / `ArtifactStore` / `LLMProvider` / `ControlBroker`。单进程同步,无队列。

**为什么存储用了两种,而不是统一一种:**

| 存什么 | 谁在读写 | 用什么 | 理由 |
|---|---|---|---|
| artifact、日志、截图 | 一个进程,顺序写,写完不改 | 磁盘文件 | 题目要求 artifact 可审阅、可版本化,而 git 现成就能做到这两件事:改了能看 diff,能在 PR 里逐行评论 |
| 控制权状态 | 自动化进程和多个操作台同时争抢 | SQLite | 需要"当前是 PAUSED 才能改成 HUMAN"这种带条件的原子更新。在 JSON 文件上做这件事得自己写跨平台文件锁,出错风险高于收益 |

两者的读写模式不同,所以不该用同一种存储。SQLite 不算额外基建:标准库自带,一个文件,不用起服务。

## 1.6 关键决策与拒绝的替代方案

这张表是整份文档的索引。九处拒绝,基本覆盖面试官会提议的全部替代方案。

| 决策 | 拒绝了什么 | 拒绝理由 | 原则 |
|---|---|---|---|
| a11y 优先 | DOM 优先 | DOM 只有浏览器有,桌面用不了 | P1 |
| 定位有序,首个唯一命中者赢 | 加权打分取最高分 | 权重无来源;同输入可能走不同层,破坏确定性;混合分数抹掉降级信号 | P2 P3 |
| 可靠性运行时统计 | artifact 里写静态 confidence | 数字无来源 | P3 |
| ordinal 仅限容器内 | 页面级 ordinal | 租户加一个字段就全错位 | P1 |
| 接口只加 `wait_for` / `location` | 加 frame 切换 | 只有网页有 frame,概念泄漏 | P1 |
| 同上 | 加 dialog 专用方法 | dialog 就是 `role=dialog` 节点,三个方法够 | P1 |
| broker 用 SQLite 文件 | 起一个 HTTP broker 服务 | 人点的是眼前的窗口,不经 broker 代理;为存一行状态起进程属不被奖励的扩展基建 | P7 |
| 异常靠 hardening 跑出来 | prompt 里预给业务结果清单 | 那等于人写答案让模型填,分类就不是发现的了 | P3 |
| overlay 三种 op + 静态校验 | slots | slot 要求 base 预知租户在哪扩展,破坏单向依赖 | P7 |
| headed + 像素流通道 | 暴露远程调试端口 | 调试协议给的是任意脚本执行,等于在已登录 session 上开任意代码执行面 | P5 |

## 1.7 测试范围

只测四样,都是纯逻辑不开浏览器,跑得快:

schema 校验、定位器解析、错误分类、脱敏。

这四样恰好是评分表上权重最高的部分。浏览器交互不做自动化测试,evidence 目录是更强的证明。

## 1.8 为什么用 Groq

*一句话:我们给模型看的是一份文字列表,不是截图,所以不需要"会看图"的模型,只需要它能输出结构化动作。*

| 理由 | 说明 |
|---|---|
| 不需要视觉能力 | 感知走 a11y 树(P1),模型收到的是"可交互元素列表",要它做的是 tool calling,不是算坐标。专门训练过看屏幕的模型在回答一个我们没问的问题 |
| 快 | 一次 discovery 是好几次串行的模型调用,而这个循环靠反复重跑来调。延迟低直接等于迭代快 |
| 免费额度够 | 只需要跑通几次成功的 discovery,成本接近零 |

**两条连带影响,主动写出来而不是等被发现:**

免费额度有限流,所以每一步发给模型的观测要裁剪成只有可交互元素,截图只当 evidence 不当输入。这恰好是感知设计本来就想要的。

托管的模型 catalogue 是轮换的,preview 模型可能随时下线。所以**这套系统里的契约是 `LLMProvider.decide()`,模型 id 是配置不是架构。**

被问"为什么不用 Anthropic 的 computer-use"时,答案就是上面第一行加最后一句:感知方式不需要它,而且换过去只是加一个类。

---

# 第二部分 分

---

## 2. Artifact

> **结论**:artifact 是契约,不是流水账。它被三个读者共用,所以按读者分块。

### 2.1 结构

*一句话:artifact 就是一份写得很仔细的操作说明,分六块,下面这棵树是它的目录。*

```
ARTIFACT
│
├─ 1 身份与范围     schema_version / capability_id / version
│                   status / app_profile
│
├─ 2 inputs         调用方要传什么
│   └─ name / type / pattern / required / sensitivity
│
├─ 3 outputs        调用方能拿到什么
│   └─ name / type / sensitivity
│
├─ 4 possible_outcomes   可能返回哪些业务结果码
│
├─ 5 steps[]        有序步骤
│   ├─ id / action / risk_level
│   ├─ target ──► strategies[]  有序四层,不打分
│   ├─ value_from / into         按引用绑值,不存字面量
│   ├─ wait                      条件式,有界
│   └─ checkpoint                这步的后置断言
│
└─ 6 运行时
    ├─ runtime_matches[]   已知的非happy-path状态
    │   └─ category / terminal / detect / result 或 recovery
    ├─ recovery_budget     恢复的全局上界
    └─ success_condition   整个 capability 的成功判定
```

### 2.2 为什么这样分块:三个读者

*一句话:同一个文件有三种人要读,各读一部分,所以按读者分块。*

| 读者 | 读 | 不读 |
|---|---|---|
| 调用方 agent | 1 2 3 4 | steps、runtime_matches |
| Replay Engine | 全部 | |
| 人类 review | 1 2 3 4 + steps 的 risk_level | detect 细节 |

`possible_outcomes` 单独存在,就是为了第一行那个读者:**调用方要能在调用前知道会返回什么,不必读检测规则。** 就像 HTTP 客户端不需要读服务端代码才知道 404 是可能的。

里面只列**业务结果码**,不列 FAILURE 那一组:业务结果是"这个能力能合法回答什么",每个能力各不同;FAILURE 是系统级的,每个能力都一样,不用重复声明。

本项目实现的那一个能力,列表是:`MEMBER_NOT_FOUND` / `ACCOUNT_FROZEN` / `PERMISSION_DENIED` / `SESSION_LOST_DURING_HANDOFF`。

### 2.3 字段用途

*一句话:下面每一行回答"这个字段存在是为了干什么"。*

| 部分 | 字段 | 干什么 |
|---|---|---|
| 身份 | `schema_version` | 这套格式的版本 |
| | `version` | 这个能力的版本,流程变了就加 |
| | `app_profile.version` | 录制时对面厂商产品的版本 |
| | `status` | `draft` / `approved`,门控无人值守回放 |
| inputs | `sensitivity` | logger 读它决定脱不脱敏,不靠人记得 |
| | `pattern` | 执行前校验,坏参数不进浏览器 |
| outputs | `type: money` | `{amount_minor, currency}`,不用 float |
| | `sensitivity` | 出站的姓名也是 PII |
| steps | `risk_level` | 执行器判定后覆写,不接受自报 |
| | `target.strategies[]` | 有序,第一个唯一命中者赢 |
| | `value_from` / `into` | 只存引用,真实值结构上不在文件里 |
| | `wait` | 条件式等待,超时进 recoverable |
| | `checkpoint` | 这步的后置断言,失败触发分类 |
| 运行时 | `terminal` | 决定匹配顺序,terminal 的先匹配 |
| | `category` | 三分法的归属 |
| | `recovery_budget` | 见 3.4 |
| | `success_condition` | 每步 checkpoint 证明步骤落地,这个证明能力交付 |

三个 version 不可混用:`schema_version` 是格式,`version` 是能力,`app_profile.version` 是外部产品。**只有最后一个是关于外部世界的事实**,前两个是我们自己的。

### 2.4 引用的三个命名空间

*一句话:文件里不存真实的值,只存"到时候去哪儿取"。取值的地方有三种,规矩不同。*

| 命名空间 | 来源 | 文件里存什么 |
|---|---|---|
| `input.*` | 调用方每次传入 | 只存引用 |
| `ctx.*` | 本次运行从页面读到 | 只存引用,值只在内存 |
| `output.*` | 返回给调用方 | 只存声明 |

只存引用不存字面量,一个机制同时解决两件事:**参数化**(同一份 artifact 查任何客户)和**PII 不落盘**(录制时用的真实客户号结构上不在文件里)。无可脱敏强于脱敏。

`ctx` 是为闭环事务准备的。只能消费调用方传入的值,这套系统就只做得了查询:

```yaml
- id: s2
  action: extract
  into: "ctx.txn_token"            # 页面生成的流水号

- id: s4
  action: type
  value_from: "{{ctx.txn_token}}"  # 下游填回去
```

`ctx` 的值全部来自真实屏幕,所以**默认当敏感处理,不靠 tag 声明**:不以原文进日志。它是唯一一类敏感性不在 artifact 里声明的引用。

### 2.5 定位分四层

*一句话:怎么在界面上找到那个控件,有四种办法,按可靠程度排队,不行就往下退。*

有序,第一个唯一命中者赢,记录命中层。

| 层 | 策略 | 理由 |
|---|---|---|
| 1 | role + accessible name | web 和 desktop 共有,可移植 |
| 2 | 相对标签锚定 | 控件无可访问名时用,legacy 常见 |
| 3 | 稳健 CSS | 仅 web,排除生成 id |
| 4 | 视觉 bbox | canvas / 图片界面兜底 |

排序本身就是论证:**所有可移植的都排在所有浏览器专属的之上。** 层 3 层 4 由 web adapter 声明,其他 adapter 不 advertise。

**匹配到多个元素 = 未命中**,跳下一层,不用位置消歧。这条是"页面级 ordinal 不进主路径"的直接来源。

拒绝加权打分的三条理由(见 1.6),核心是第二条:加权之后,同一份 artifact 同一个输入,markup 轻微变化就可能换一条执行路径。那正是回放路径存在的目的要消除的东西。

### 2.6 完整示例

*一句话:上面讲的东西拼起来长这样。*

```yaml
schema_version: "1.0"
capability_id: "acme_core.lookup_savings_balance"
version: 3
status: draft | approved
app_profile:
  product: acme_core
  version: "2024.1"

inputs:
  - name: member_id
    type: string
    pattern: "^[0-9]{5}$"
    required: true
    sensitivity: pii

outputs:
  - name: savings_balance
    type: money               # { amount_minor: int, currency: str }
  - name: account_status
    type: enum
    values: [ACTIVE, FROZEN, CLOSED]
    sensitivity: none

possible_outcomes: [MEMBER_NOT_FOUND, ACCOUNT_FROZEN, PERMISSION_DENIED, SESSION_LOST_DURING_HANDOFF]

steps:
  - id: s2
    action: type
    risk_level: REVERSIBLE_WRITE
    target:
      strategies:                         # 有序,不打分
        - { layer: 1, type: role_name, role: textbox, name: "Member ID" }
        - { layer: 2, type: label_anchor, label: "Member Number:" }
        - { layer: 3, type: css, selector: "#frmSearch input[name=f3]" }
    value_from: "{{input.member_id}}"
    wait: { until: element_visible, timeout_ms: 8000 }
    checkpoint: { type: field_value_equals, ref: "{{input.member_id}}" }

runtime_matches:
  - id: member_not_found
    category: business_outcome
    terminal: true                        # terminal 的先匹配
    detect: { text_contains: "No member records match" }
    result: { code: MEMBER_NOT_FOUND }
  - id: session_warning
    category: recoverable
    terminal: false
    detect: { role: dialog, name: "Session Warning" }
    recovery: { action: click, target: { role: button, name: "Keep Alive" } }
    max_retries: 2

recovery_budget:
  per_run: 5                              # 整次运行恢复总次数
  max_depth: 1                            # 恢复动作内部不再走恢复分支

success_condition:
  all_of: ["output.savings_balance != null"]
```

---

## 3. 确定性与错误处理

> **结论**:执行路径固定;checkpoint 失败不等于失败,它只是触发分类。

### 3.1 单步执行顺序

*一句话:点完一个按钮之后,系统按下面的顺序判断这一步成没成。*

```
act → wait(条件式,有界;无裸 sleep)
    → observe(只一次,后续全用这份快照)
    → 先匹配 terminal runtime_matches
         business_outcome → 干净返回
         recoverable      → 执行 recovery,本步重试(有界)
    → 再断言 checkpoint
         成立   → 提交,下一步
         不成立 → 用同一份快照匹配其余 matcher
                    命中   → 按类分流
                    未命中 → FAILURE + 存证 + 升级
```

三个要点:

**一次 observe,两个用途。** 不读两遍页面,也避免两次之间页面变了导致判断互相矛盾。

**terminal 排在 checkpoint 之前。** 页面显示"查无此人"时 checkpoint 永远不会成立,等它超时纯属浪费,而且根据分支不同还可能被误报成失败。**反过来的顺序就是"查无此人变成崩溃"的具体成因。**

**`wait` 和 `checkpoint` 是两件事。** `wait` 等页面稳定,超时进 recoverable;`checkpoint` 断言状态成立。两者都来自 artifact,执行器只知道怎么断言,不知道该断言什么。URL 变了 / 返回 200 / click 没抛异常,都不算证据。

### 3.2 三分结果

*一句话:跑完的结果分三类,不是"成功"和"失败"两类。中间那类最容易被做错。*

| 类别 | 例子 | 返回 |
|---|---|---|
| 业务结果 | 查无此人、账户冻结 | `BUSINESS_OUTCOME` + code |
| 可恢复 | 会话弹窗、慢加载 | 对调用方透明,记日志 |
| 硬失败 | 500、定位层耗尽、未知阻断弹窗 | `FAILURE` + step + expected + observed |

```json
{ "status": "SUCCESS", "outputs": { "savings_balance": { "amount_minor": 815000, "currency": "USD" } } }
{ "status": "BUSINESS_OUTCOME", "code": "MEMBER_NOT_FOUND" }
{ "status": "FAILURE", "code": "CHECKPOINT_FAILED", "failed_step": "s3",
  "expected": "heading 'Member Detail' visible", "observed": "text 'System Error 500'",
  "evidence": "evidence/replay-r004/s3.png" }
```

第一类最关键:客户号不存在,是**对调用方问题的正确回答**,不是异常。当成故障处理会让运维被假故障淹没,真故障反而被埋。

### 3.3 runtime_matches 从哪来:hardening pass

*一句话:AI 第一次跑一路顺利,没见过任何异常,所以"遇到查无此人怎么办"这段是空的。补法是再跑一次,故意填错的客户号。*

happy path 那次没见过任何异常,所以直接产出的 artifact 业务分支是空的。**文档承诺的功能会没有来源。**

所以 discovery 分两阶段:成功之后用坏参数再跑一次,记录分叉步和分叉后的状态。

```
第一次 member=12345 → s3 之后到"客户详情"页
第二次 member=99999 → s3 之后到另一个页面,写着"查无此人"
                      → 把这个特征写进 runtime_matches
```

**分类由分叉行为的机械性质推出,不由模型判断,也不在 prompt 里预给清单:**

| 观察 | 归类 |
|---|---|
| 稳定渲染的终态页,无错误信号 | business_outcome |
| 错误页 / 渲染失败 | hard_failure |
| 自行消失或按声明动作可关闭 | recoverable |

code 名字(`MEMBER_NOT_FOUND`)是 curation 时人贴的标签,也是 `draft` → `approved` 前人唯一要确认的东西。**命名审起来便宜,分类审起来不便宜**,所以分类是推导的,命名不是。

这一步不用模型,只要几步,而且顺手产出第三份 evidence。

### 3.4 恢复的三重上界

*一句话:自己处理小毛病是好事,但要防止它无限循环。三道上界,最后一道靠结构而不靠计数。*

recovery 动作声明在检测到该情况的 `runtime_matches` 条目上,不挂在某个 step 上,所以一个弹窗描述一次,出现在哪一步都能处理。

| 上界 | 值 | 作用 |
|---|---|---|
| `max_retries` | 每个 matcher 2 | 同一情况反复出现的次数上限 |
| `recovery_budget.per_run` | 5 | 整次运行所有恢复加起来的上限 |
| `recovery_budget.max_depth` | 1 | **恢复动作内部不再走恢复分支** |

深度那条是关键:recovery 执行完就回主流程重试该步。该情况再出现,计入该 matcher 的 retries,用完即硬失败。**震荡在结构上不可能发生**,不靠计数拦。计数恰恰是嵌套恢复能绕过的东西。

这一类同时吸收了看起来像条件分支的需求。"每月首次登录弹合规提醒,平时不弹"不需要分支:它就是一条 recoverable matcher,弹了就点掉,不弹就匹配不上。**可选弹窗是恢复问题,不是控制流问题**,这也是这个类别存在的主要理由。

### 3.5 漂移

*一句话:界面真变了,我们的做法是停下来报告,不是悄悄适应。*

每次解析记录命中层。持续从 layer 1 降到 layer 2 → 标记该 artifact 待复查。

**不在运行时自愈。** 悄悄适应正是 artifact 要消除的非确定性。界面真变了,做法是停下来报告。

---

## 4. 异构与多租户

> **结论**:设计,不实现。判断这条缝是否真实的标准只有一条:接口之上有没有出现浏览器专属概念。

### 4.1 seam:SurfaceAdapter

*一句话:所有"碰浏览器"的代码只能写在这一个类里,上层代码不知道下面是浏览器还是桌面软件。这条分界线就是题目问的 seam。*

```python
class SurfaceAdapter(Protocol):
    def observe(self) -> SurfaceSnapshot: ...          # 归一化节点树 + 可选截图
    def resolve(self, target: Target) -> Handle: ...
    def act(self, handle: Handle, action: Action) -> None: ...
    def wait_for(self, pred: Predicate, timeout_ms: int) -> bool: ...
    def location(self) -> Location: ...                # web: route;desktop: window + view
```

seam 之上只用每种界面都有的词汇:role、accessible name、相对标签、text。

两个方法值得单说:

- `wait_for` **必须在 seam 上**:P2 禁止裸 sleep,那等待就必须是可声明、可记录、有界的一等操作,不能藏在 `resolve` 里偷偷做
- `location()` **而非 `current_url()`**:allowlist 检查和漂移指纹都需要知道 session 在哪,但 URL 是浏览器事实,desktop adapter 只能造假

### 4.2 接口的准入规则

*一句话:这个接口里不许出现任何只有浏览器才有的东西,否则以后加桌面支持就加不上了。*

**desktop 或 canvas adapter 无法诚实实现的东西,不进接口。**

据此拒绝两个:frame 切换(仅 web 有,`WebAdapter` 内部解决)、dialog 专用方法(dialog 就是 `role=dialog` 节点,现有三个方法够)。

两个都考虑过。加任何一个,就把浏览器概念放到了 seam 之上,**第 4 节全部论证立刻变成假的。**

实现 `WebAdapter`。`DesktopAdapter`(Windows UIA / macOS AX)、`VisionAdapter` 只留接口。它们可信而非空谈,理由就是上面这条规则:seam 之上没有任何东西提到 CSS 选择器、frame 或 DOM 节点。

### 4.3 多租户:base + overlay

几百家机构跑同一个厂商产品,界面大同小异,只是品牌、标签、版本不同。每家重录一遍意味着几千份 artifact,而且厂商升级一次,几千份全部作废。

**做法:写一份通用的(base),每家只记自己的不同之处(overlay)。**

```
base(通用版,只有一份)
  s1 打开搜索页
  s2 填客户号
  s3 点搜索
  s4 读余额

  ├─ A 银行的 overlay:s2 那个框叫"会员编号",不叫"客户号"
  ├─ B 银行的 overlay:空的,直接用通用版
  └─ C 银行的 overlay:s2 之后多一步"选网点"
```

**三种改法,不是一种。**

| op | 意思 | 什么时候用 |
|---|---|---|
| `replace_target` | 换一个控件的找法 | 这家的输入框换了个叫法 |
| `insert_after` | 在某步之后插一步 | 这家多一个必填字段 |
| `skip` | 跳过某一步 | 这家没有确认页 |

真实的租户差异不只是改标签:有的机构多一个必填字段,有的省掉确认页。只能改叫法的 overlay 表达不了后两种,那就得为这些情况重录,而分层机制存在的目的正是避免重录。

```yaml
extends: "acme_core.lookup_savings_balance@3"
tenant_id: cu_first_national
overrides:
  - op: replace_target
    step_id: fill_member_id
    target:
      strategies:
        - { layer: 1, type: role_name, role: textbox, name: "Acct / Member #" }
  - op: insert_after            # 该租户多一个必填的网点下拉框
    step_id: fill_member_id
    step: { id: select_branch, action: select, ... }
  - op: skip                    # 该租户没有确认页
    step_id: confirm_page
```

**性质一:依赖单向。**

overlay 里写着它基于哪个 base;base 里不写有哪些 overlay。

```
A 银行 overlay ──指向──► base
B 银行 overlay ──指向──► base
C 银行 overlay ──指向──► base
                          base 不知道这三家的存在
```

好处:新接一家机构,只新建它自己的 overlay 文件,**不用动 base**。反过来的话,每接一个客户都要改那个所有人共用的文件。

**性质二:只存差量。**

overlay 里只写不一样的地方,不抄一遍全部步骤。所以厂商升级时改一次 base,所有机构都跟着好了。如果每家都存完整副本,你得改几百份。

**风险:跳过一步可能把下游搞崩,而文件上看不出来。**

假设某家没有确认页,overlay 里写 `skip: s3`。但如果 s3 是"读一个流水号",而 s5 要用它:

```
s3 读流水号 → 存起来
s4 ...
s5 把流水号填进去      ← 依赖 s3
```

跳过 s3 之后,s5 要用的东西没人产出了。流程必崩,**而 overlay 文件里只写了 "skip s3",完全没提 s5**。

**解法是加载时先检查一遍,不是等跑到 s5 才发现。**

```
每一个"要用某个值"的步骤 → 产出那个值的步骤还在不在?
每一个提到的步骤编号     → base 里真的有吗?

任一条不满足 → 拒绝加载这个 overlay,根本不让它开始跑
```

**为什么不用 slots。**

slots 是另一种做法:base 里预留几个插槽,租户往里塞东西。

```
base:
  s1 打开搜索页
  s2 填客户号
  【插槽 A】        ← 预留,给租户插东西
  s3 点搜索
```

问题是 base 得**提前猜到**租户想在哪插东西,那 base 就得考虑租户的情况,正好违反上面的性质一。

而 `insert_after: s2` 不需要预留:overlay 自己说"在 s2 后面插一步",base 完全不用配合。

### 4.4 漂移检测与优雅降级

**第一件事:提前发现界面变了。**

定位有四层,第 1 层最可靠,不行才退到第 2 层。**每次回放都记一笔:这一步是靠第几层找到的。**

```
A 银行,这周:  s2 → 第 1 层命中  ×3
A 银行,下周:  s2 → 第 2 层命中  ×3     ← 变了
```

第 1 层是按名字找,一直好用,现在突然不行了。说明这家把那个输入框的名字改了,可能是升级了系统。

现在还能跑,因为第 2 层兜住了。但这是预警:再变一次可能就兜不住。

所以标出来让人去看一眼,**而不是等某天彻底跑不动、变成生产事故才发现。**

**第二件事:套不上的时候,顺手把修复方案攒出来。**

通用版套到某家头上,某一步四层全找不到,不是直接崩掉报错,而是走第 5 章那条人工接管的路:

```
1. 停下来,叫人
2. 人在浏览器里手动把那一步做完
3. 系统把人做了什么记下来(前后截图、页面变化、一句说明)
4. 流程继续跑完
```

关键在第 3 步。**人记下来的东西,正好就是"这家跟通用版不一样在哪"。**

拿着这个记录直接能写出这家的 overlay:

```yaml
# 从人的操作记录整理出来
overrides:
  - op: replace_target
    step_id: s2
    target: { name: "Acct / Member #" }   # 人当时点的是这个框
```

所以出问题那一次不是白跑的:**它顺手把"怎么修"也给出来了。** 普通系统遇到套不上只会说"失败了,你自己看"。

### 4.5 假银行故意制造四类标记

只有靶机足够烂,四层降级才是被验证的而不是被声称的。

| 标记 | 逼出 |
|---|---|
| `<label for>` + 真 `<button>` | layer 1 |
| 表格单元格 `Member Number:` 旁边无 label 的 `<input id="ext-gen-1029">` | layer 2 |
| `<div onclick>` 当控件,无 role 无 aria-label | layer 3 |
| 纯 `<img src="tab_sav.gif">` 无 alt 的导航 tab | 全部耗尽 |

**最后一行是故意的**:没有一个所有层都到不了的目标,就产不出 FAILURE 和升级的 evidence,那整份 demo 只能证明 happy path 能跑。

注意带 role + aria-label 的控件是**无障碍最佳实践,不是 legacy**,它考 layer 1 不考降级。

**假银行还要能触发结果契约的每一条分支,不只是成功那条。**

| 造什么 | 逼出哪个结果 |
|---|---|
| 客户号 12345 存在 | `SUCCESS` |
| 客户号 99999 无记录 | `MEMBER_NOT_FOUND` |
| 客户号 12346 已冻结 | `ACCOUNT_FROZEN` |
| 客户号 12347 超出权限档 | `PERMISSION_DENIED` |
| `?inject=timeout` 弹会话警告 | recoverable |
| `?inject=notice` 弹合规提醒 | recoverable |
| `?inject=slow` 延迟 15 秒 | 测 `wait` |
| `?inject=500` 直接报错 | `FAILURE` + 升级 |
| 详情页上一个"改额度"按钮 | `IRREVERSIBLE` 被执行器拒绝 |

**能按需触发这些,是目标应用必须自己写的唯一理由。** 公开 demo 站没法让它超时、没法变出第二个租户版本,还要守人家的条款和速率限制。可控性是理由,省事不是。

这样**一个 capability 就把设计里所有分支都跑遍了**,不需要做第二个能力。数据全是假的,题目禁止真实凭据和真实 PII。

### 4.6 本节砍掉的

砍:overlay 继承解析器、漂移看板。

留:schema、一份 artifact 跑两个界面版本、命中层 telemetry。

---

## 5. 升级与接管

> **结论**:机制是真的,通道是简化的。这两件事必须分开讲。

### 5.1 卡住判定

*一句话:什么情况算"跑不下去了",四条,都是明确条件,不靠干等超时。*

四条显式触发,不靠超时:

- 某步所有定位层耗尽
- checkpoint 失败且状态不匹配任何 runtime_matches
- 步骤为 `IRREVERSIBLE`,按策略需人工授权
- discovery 期连续 N 步状态无变化

### 5.2 介入请求带什么

*一句话:叫人的时候不能只说"出错了",要把人上手需要的信息都带上。*

capability、goal、run_id、step_id、reason code、expected vs observed、截图、已完成步骤、session handle。

脱敏规则同日志,一视同仁。

### 5.3 机制和通道是两件事

*一句话:"人接管同一个 session"这件事是真做的;"人怎么看到那个窗口"这件事是简化的。这两件不能混着说。*

| | 是什么 | 这一版 | 生产 |
|---|---|---|---|
| 机制 | 自动化停止触碰但保活 session,人驱动同一个 session | 真做 | 同样 |
| 通道 | 人怎么看到并操作那个窗口 | 本机 headed 窗口 | 同容器内 Xvfb + noVNC 给集中控制台 |

机制与显示方式无关,在容器里同样成立。通道是刻意简化,换掉它不影响上面任何东西。

**不提议为此暴露远程调试端口。** 调试协议给的是页面内任意脚本执行和任意跳转,把它交给运维控制台,等于在一个已登录的银行 session 上开了一个任意代码执行面,不是"多开一条通道"而已。而且没必要:运维要的是画面和输入,像素流给得了,且不附带这些权限。

**接管细节**:headed 窗口已开着。自动化转 `PAUSED` 并停止触碰页面,窗口、cookie、表单状态原样停在该步。人直接在那个窗口操作,完成后发信号。

不重启不重填,因为**部分完成的工作通常不幂等**:重跑一个已经生成了待处理单号的流程,会生成第二个。

### 5.4 控制权

*一句话:自动化和人不能同时点同一个浏览器,所以要有个地方记着"现在谁在开车"。下面三条机制各防一种出错。*

```
AUTOMATION --stuck--> PAUSED --claim--> HUMAN --release--> RESUMING --> AUTOMATION
                        ↑                  |
                        +--- lease 过期 ----+
```

broker 是一个 SQLite 文件,不是服务。拒绝 HTTP broker 的理由见 1.6。

**三条机制,各答一个具体失败:**

**claim 是条件原子更新,不是写入。** 过期惰性判定,不需要定时任务:

```sql
UPDATE control SET state='HUMAN', holder=?, lease_expires_at=?
WHERE run_id=?
  AND (state='PAUSED' OR (state='HUMAN' AND lease_expires_at < ?));
```

看影响行数。两人同抢,第二个匹配零行。

单进程架构下靠调度器收租约,要么需要一个不存在的组件,要么在它死掉时正好失效。**惰性过期不需要任何东西在运行。**

**控制权是租约不是所有权。** 持有者拿到的是有界窗口,必须续租。人走开或进程死了,租约过期后回到 `PAUSED` 让别人接,而不是永远卡在 `HUMAN`。

**执行器每个动作前复检持有者。** 持有 token 不够。中途变更时最多多执行一个动作就停,不和人抢 DOM。代价是每步多一次读,相对每步本来就有的 wait 可忽略。

### 5.5 Resume 只查两个候选

*一句话:人交还控制权之后,系统不能瞎猜自己在哪。只查两件事:整件事做完了吗,卡住那一步做完了吗。*

不按下一个 step index 续跑,**也不扫描全部 checkpoint。**

checkpoint 天然会碰撞:"在客户搜索页"既是 s1 之前的状态,也是查询失败后的状态,也是流程走完回到首页的状态。扫全部只会命中多个,无从选择。

| 序 | 查什么 | 成立则 |
|---|---|---|
| 1 | 顶层 `success_condition` | 人把整件事做完了。返回 `SUCCESS`,后续步骤一步不执行 |
| 2 | 卡住那一步的 checkpoint | 人把这一步做完了,从下一步继续 |

两个都不成立 → 二次升级,不猜。

第 1 条同时答了"运维顺手把事做完了"这个常见情况:不能再跑一遍后续步骤,部分完成的工作通常不幂等。

**把搜索范围限定在"已知卡住的那一步"加"终态条件",resume 才是可判定的**;通用的"我现在在哪"搜索不是。

### 5.6 会话超时与接管抢时间

*一句话:银行系统几分钟不操作就把你踢出去,而等人来接管往往更久。所以要保活,保不住就老实说 session 丢了。*

银行后台通常几分钟无操作就踢,而派单、等人认领、人读屏思考加起来可能更久。**假设 session 能扛过接管,等于把最可能发生的情况假设掉了。**

两条措施,都不需要凭据:

**保活。** `PAUSED` 且尚无人认领期间,执行器对 allowlist 里专为此声明为 `SAFE_READ` 的路由做周期性 ping。它是读,不改状态,只为了让无操作计时器不归零。

**承认保活会失败。** session 丢失是一个声明出来的结果,不是假装无损恢复:

```
SESSION_LOST_DURING_HANDOFF  + 已完成的步骤区间
```

**自动重新登录刻意不做。** 两条理由:要求系统持有凭据(第 6 章禁止);重登产生的是一个新 session,而"人接管的必须是同一个 session"正是题目 3.6 的硬要求。

诚实报告 session 没了,强于恢复到一个不是当初那个的 session。这是 P6。

### 5.7 mock 的部分

*一句话:给人用的界面是假的(命令行),但底下交接控制权那套机制是真的。*

operator console 是 CLI:`ops claim <id>` / `ops release <id>`。

真做的是:状态机、条件 claim、租约惰性过期、动作前复检、基于 checkpoint 的 resume。

**一处诚实的限制**:人的动作记录为时间窗 + 前后截图 + URL 变化 + operator note,不是语义事件流。抓原始输入事件可行,但可靠映射回语义控件在此范围内不是已解决问题,声称能做就是声称超出代码的能力。

---

## 6. 安全

> **结论**:四层,全在执行器强制。artifact 是数据,数据没资格给自己的动作授权。

### 6.1 两个拦截点,位置不可互换

*一句话:护栏有两道,一道在动作之前,一道在写文件之前。位置换了就全失效。*

```
decide → [allowlist + risk tier] → act → observe
                                            ↓
                        log / screenshot → [redaction] → evidence/
```

| | 放在动作前 | 放在写盘前 |
|---|---|---|
| allowlist | **正确** | 没用,动作已经执行了 |
| redaction | 没用,那时还没有日志 | **正确** |

画成并排一行会让人以为这是同一个组件干的两件事,而它们中间隔了整个执行过程。

### 6.2 allowlist

域名、路由模式、动作类型。名单外一律拒绝。

**白名单而非黑名单**,因为黑名单列不完。

步骤指向名单外目标 → 拒绝执行,记为带专用 code 的硬失败,**不管 artifact 怎么写**。

### 6.3 risk tier

*一句话:动作按危险程度分三级,不可逆的那一级默认不许自动做。*

由执行器从 action type + target 判定,**不接受 artifact 自报**,否则错标的步骤能给自己降级。

| 级 | 例 | 无人值守 |
|---|---|---|
| `SAFE_READ` | read / extract / paginate | 允许 |
| `REVERSIBLE_WRITE` | 填搜索词、切 tab | 允许 |
| `IRREVERSIBLE` | 提交转账、关闭账户、改额度 | 默认拒绝 |

默认拒绝,配置开启后须走第 5 章的人工授权路径。

**理由是后果不对称**:挡住一个合法动作,成本是运维一分钟;核心银行系统里一次未授权的不可逆动作,无法靠重试挽回。

### 6.4 执行上界

*一句话:最多跑多少步、多久,都要有硬上限。跑不停不只是浪费钱,它是在反复点一个真实的银行系统。*

max steps、单次运行墙钟上限、每个 wait 超时、每个 recoverable 的重试上限、discovery 期 N 步无进展。

触上界即终止 + 存证 + 升级,**绝不静默继续**。

跑不停不只是成本问题,**它是护栏失效**:一个无上界的循环在反复点一个真实的后台系统。

上界和 allowlist 放在一起,理由相同:**一个需要申请许可才能动作的组件,不该是那个给自己设上限的组件。**

### 6.5 敏感数据:两套独立机制

*一句话:一套让客户信息根本不进文件,一套兜底把漏出来的遮掉。*

**结构上。** artifact 按引用绑值,录制时的具体值本就不在文件里。**无可脱敏强于脱敏。**

**运行时。** logger 按 `sensitivity` 标记脱敏,加正则兜底(SSN / PAN)。

**遮值不遮字段**:`member_id=<pii:5 digits>`,不用哈希也不省略。否则同一 capability 两次运行的日志无法区分,不可调试,而 3.5 节要求 evidence 能解释一次运行。关联靠 run_id,run_id 不敏感。

失败截图按敏感产物处理:存在 run 目录下、排除出 git、绝不内联进日志。

**outputs 也标 sensitivity,不只 inputs。** 返回的客户姓名出站是 PII,和入站的客户号一样。

### 6.6 限制,明说

- allowlist 管导航不管语义。允许路由内做错事它拦不住,只能靠 risk tier 和 review
- 正则脱敏对异常格式有漏网
- evidence 目录无静态加密
- `approval` 只是 artifact 里的字段,不是强制流程。生产需要签名和 review gate,见 7.3 第一条

---

## 7. 砍掉的

> **结论**:每一条都是刻意省略且留了缝,不是没做完的项。

### 7.1 留了接口

| 砍 | 为什么 | 留了什么 |
|---|---|---|
| desktop / vision adapter | 题目不要求;论点是缝不是第二个实现 | `SurfaceAdapter` protocol,其上无任何浏览器概念 |
| overlay 继承解析器 | schema 定对了,解析是机械的;判断在 schema | overlay schema + 一份 artifact 跑两个界面版本 |
| 漂移看板 | 报表基建,不是设计 | 每次运行的命中层 telemetry |
| operator web console | 题目明确允许 mock | CLI claim/release,对接真 broker |
| 队列 / worker / 服务拆分 | 题目明说不奖励;重要的是边界 | 四个 seam 的四个接口 |
| Postgres / 对象存储 | 此规模下文件已满足可审阅和版本化 | `ArtifactStore` 三个方法 |

### 7.2 直接砍

**artifact 里的通用条件分支。** 步骤列表表达不了"结果多于一条时点其中一行"。加条件、跳转目标、循环检测,artifact 就从一份可审阅的清单变成一门小语言,而题目 3.2 要的是人能在 PR 里 approve 的文件。

立场是**一个 capability 一条路径**:多结果里选一个是另一个 capability(`select_member_from_results`),由调用方组合,不是这个里面的分支。

看起来像分支但其实不是的两类已经有解:终态分叉是 business outcome(3.3),可选弹窗是 recoverable(3.4)。

**session 丢失后自动重登。** 见 5.6。要持有凭据,且产出的是新 session,改为返回结果码。

**回放失败时的 LLM 辅助恢复。** 有吸引力,也是题目列出的 stretch goal,但它把模型重新引入生产路径。要安全需要严格步数预算 + 动作策略检查 + 存证,宁可不做也不做糙。

**多次运行的 flakiness 打分。** 需要足够样本才有意义。喂它的机制(命中层 telemetry)已在。

**接管期间人工动作的语义捕获。** 见 5.7。

### 7.3 接下来按序做

1. **approval gate。** artifact 已有 `draft | approved`,让无人值守回放拒绝非 approved,promote 需签名 review。**改动最小,安全收益最大。**
2. **overlay resolver + 漂移报告。** 从 demo 变成跨机构可运营的分界点。
3. **对一个真实 legacy 桌面程序做 desktop adapter。** 缝是为它设计的,但**论证不是测试**,这是设计真正会被验证的地方。
4. **有界的辅助恢复**,带它需要的预算和策略检查。

### 7.4 不是砍的

discovery 是真跑的。四次运行留证,索引在 `evidence/README.md`,本文引用的每个路径都能落到文件:

| run | 场景 | 结果 |
|---|---|---|
| `discovery-r001` | 真实模型驱动 discovery | 产出 `lookup_savings_balance.v1.json` |
| `replay-r002` | 有效客户号 | `SUCCESS` + typed outputs |
| `replay-r003` | 无记录的号 | `BUSINESS_OUTCOME / MEMBER_NOT_FOUND` |
| `replay-r004` | 注入 500 → 人工接管 → 恢复 | 先 `FAILURE` 升级,交还后 `SUCCESS` |

`replay-r004` 最值钱:它一次跑完 checkpoint 失败、分类、存证、控制权状态机、基于 checkpoint 的 resume。

题目原文明确:discovery 不能用描述代替实做。
