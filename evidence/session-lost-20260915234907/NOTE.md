Real replay() engine, a REAL SQLite ControlBroker (two independent connections, one per thread, exactly as production cua replay / cua ops does across two processes), and real cross-thread claim/release -- only the SurfaceAdapter is scripted, same technique as tests/test_escalation_wiring.py::test_session_lost_is_reported_when_the_resumed_page_is_out_of_scope.

Why scripted: the mock target app has no session-expiry/redirect mechanism to trigger this live (see REPORT.md sec 2/5) -- there is no real fixture that lands a real browser outside the allowlisted scope. The adapter's location() reports an off-scope origin after the simulated crash, standing in for a login redirect during the handoff wait; everything else in this run -- the broker's state machine, the claim/release calls, find_resume_point's own check, the app_profile-aware allowlist check that actually classifies this as session_lost -- is the real code, not mocked.

Also real, as a side effect of going through the actual escalation path: `intervention.json` and `human_action.json`, including the operator's real `--note`-equivalent text and the mechanically-derived `resume_decision: "none"` / `human_performed_pending_action: false` (see REPORT.md sec 5). Their `before`/`after.screenshot_ref` fields name paths with no file behind them, for the same reason as the sibling `recovery-refused-runtime-*` run: this scripted adapter's `screenshot()` is a no-op.